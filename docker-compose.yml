services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: gitwatch
      POSTGRES_PASSWORD: gitwatch
      POSTGRES_DB: gitwatch
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
